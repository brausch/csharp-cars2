<!DOCTYPE html>
<html>
<head>
  <meta charset='utf-8'>
  <title>To do</title>
  <link rel='stylesheet' href='https://maxcdn.bootstrapcdn.com/bootstrap/3.2.0/css/bootstrap.min.css'>
</head>
  <body>
    <h1>You've added a new car!</h1>
    <p>@Model.GetMake()</p>
    <p>@Model.GetModel()</p>
    <p>@Model.GetYear()</p>
    <p>@Model.GetMileage()</p>
    <p>@Model.GetPrice()</p>
    <p>@Model.GetDescription()</p>
    <p><a href="/">Add another Car</a></p>
    <p><a href="/view_all_cars">View current Car</a></p>
  </body>
</html>
